# spring test application default setting
spring:
  datasource:
    driver-class-name: org.h2.Driver
#    url: jdbc:h2:tcp://localhost/~/notionBoardDB
    url: jdbc:h2:mem:notionBoardDB
    username: sa
    password:
  jpa:
    database: H2
    hibernate:
      ddl-auto: create
    open-in-view: false
    show-sql: true
